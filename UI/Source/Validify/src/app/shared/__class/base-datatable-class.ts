/*
import { Output, Input, EventEmitter, ViewChild } from '@angular/core';
import { Paginate } from "app/common/class/paginate.class";
export class BaseDatatableClass {  
    filter: Paginate = new Paginate();
    constructor() { }
    onSort(event) {
        this.filter.field = event.field;
        this.filter.order = event.order === 1 ? "ASC" : "DESC";
        this.filter.page = 1;
    }
    onPageChanged(event) {
        this.filter.page = event.page;
        this.filter.itemsPerPage = event.itemsPerPage;
    }
}
*/
