
/*
import { Output, Input, EventEmitter, ViewChild } from '@angular/core';
import { ModalDirective } from 'ngx-bootstrap/modal';

export class BaseModalClass {
    @Output() openPopChange: EventEmitter<any> = new EventEmitter();
    @Input() openPop: boolean = false;
    constructor() { }
    onShown() {
    }
    onHidden() {
        this.openPopChange.emit(false);
    }
}
*/
